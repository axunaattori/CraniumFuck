/*
 * https://github.com/axunaattori/CraniumFuck
*/

void main()
{
    byte terms = 13;
    byte a = 0, b = 1, next;

    while(terms)
    {
        next = a + b;
        putchar(next); //prints in ASCII, don't mind.
        putchar('\n');
        a = b;
        b = next;
        terms--;
    }
}
